<%- include('../partials/header') %> <!-- ✅ כולל את הקובץ header – מוסיף תגיות <head> וקישורי CSS קבועים -->
  <%- include('../partials/navbar') %> <!-- ✅ כולל את תפריט הניווט העליון של האתר -->
  
  <!-- ✅ קישורים לקבצי עיצוב -->
  <link rel="stylesheet" href="/styles/payment.css" /> <!-- עיצוב ייחודי לעמוד התשלום -->
  <link rel="stylesheet" href="/styles/global.css" /> <!-- עיצוב כללי לאתר -->
  
  <!-- ✅ עטיפת כל עמוד התשלום -->
  <div class="payment-container">
    <h1>🧾 תשלום על ההזמנה</h1> <!-- ✅ כותרת עמוד בולטת -->
  
    <!-- ✅ טופס התשלום (נשלח ל־/order/confirm ב־POST) -->
    <form action="/order/confirm" method="POST" class="payment-form">
      
      <!-- ✅ שדה חבוי שמעביר את מזהה ההזמנה מה־URL -->
      <input type="hidden" name="orderId" value="<%= orderId %>" />
  
      <!-- ✅ שדה להזנת שם בעל הכרטיס -->
      <div class="form-group">
        <label for="cardName">שם בעל הכרטיס:</label>
        <input
          type="text"
          id="cardName"
          name="cardName"
          required
          placeholder="לדוג': ישראל ישראלי"
        />
      </div>
  
      <!-- ✅ שדה להזנת מספר כרטיס אשראי -->
      <div class="form-group">
        <label for="cardNumber">מספר כרטיס:</label>
        <input
          type="text"
          id="cardNumber"
          name="cardNumber"
          required
          pattern="\d{16}" <!-- תבנית לבדיקת 16 ספרות בלבד -->
          placeholder="1234 5678 9012 3456"
        />
      </div>
  
      <!-- ✅ שורה שמכילה שני שדות: תוקף ו־CVV -->
      <div class="form-row">
        <div class="form-group half">
          <label for="expiry">תוקף:</label>
          <input
            type="text"
            id="expiry"
            name="expiry"
            required
            placeholder="MM/YY"
          />
        </div>
  
        <div class="form-group half">
          <label for="cvv">CVV:</label>
          <input
            type="text"
            id="cvv"
            name="cvv"
            required
            pattern="\d{3}" <!-- תבנית לבדיקת 3 ספרות בלבד -->
            placeholder="123"
          />
        </div>
      </div>
  
      <!-- ✅ כפתור שליחה לעיבוד התשלום -->
      <button type="submit" class="btn-pay">💳 שלם עכשיו</button>
    </form>
  </div>
  
  <!-- ✅ פוטר משותף – כולל סיום ה־HTML -->
  <%- include('../partials/footer') %>
  