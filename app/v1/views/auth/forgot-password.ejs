<%- include('../partials/header') %> <!-- כולל את קובץ ה־header הכללי: מבנה HTML בסיסי, תגיות head ו־meta -->

<%- include('../partials/navbar') %> <!-- כולל את תפריט הניווט העליון (דף הבית, מוצרים, התחברות וכו') -->

<link rel="stylesheet" href="/styles/global.css"> <!-- קישור לעיצוב הגלובלי של האתר (טקסט, רקעים, מבנה כללי) -->
<link rel="stylesheet" href="/styles/login.css"> <!-- קישור לעיצוב של טפסי התחברות/הרשמה/איפוס סיסמה -->

<section class="login-section"> <!-- מקטע מרכזי של הדף – כל הדף עטוף בקלאס לצורך עיצוב -->
  <div class="login-box"> <!-- תיבת טופס התחברות/איפוס סיסמה, עם עיצוב מובנה -->

    <h2>איפוס סיסמה</h2> <!-- כותרת הדף הראשית -->

    <!-- ✅ הודעה מותנית – מופיעה רק אם קיימת הודעה msg (שגיאה או הצלחה) -->
    <% if (typeof msg !== 'undefined' && msg) { %> <!-- בדיקה אם יש הודעה להעביר מהשרת -->
      <p class="login-message <%= error ? 'error' : 'success' %>"><%= msg %></p> <!-- הצגת ההודעה עם צבע לפי הצלחה או שגיאה -->
    <% } %>

    <!-- 📩 טופס שליחת אימייל לאיפוס סיסמה -->
    <form action="/forgot-password" method="POST"> <!-- טופס שנשלח לשרת בפוסט -->
      <div class="form-group"> <!-- קבוצה של שדה אחד (label + input) -->
        <label for="email">הכנס/י את כתובת האימייל שלך:</label> <!-- תווית לשדה האימייל -->
        <input type="email" id="email" name="email" required> <!-- שדה להזנת כתובת מייל, חובה למלא -->
      </div>
      <button type="submit" class="login-btn">שלח קישור לאיפוס</button> <!-- כפתור שליחה של הטופס -->
    </form>

    <p class="register-link">נזכרת בסיסמה? <a href="/login">התחבר כאן</a></p> <!-- קישור חזרה לדף התחברות במקרה שהמשתמש כבר זוכר את הסיסמה -->
  </div>
</section>

<%- include('../partials/footer') %> <!-- כולל את הפוטר של האתר (כפתורים, קרדיטים וכו') -->
